# Jobs API Configuration
# Copy this file to .env and fill in your own values

# MongoDB Atlas Connection URI
# Format: mongodb+srv://username:password@cluster-address/database-name?appName=ClusterName
# Get your URI from MongoDB Atlas:
# 1. Go to Clusters > Connect > Connect your application
# 2. Select "Driver" as Python and version "3.12 or later"
# 3. Copy the connection string and replace username and password
MONGO_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/jobs_db?appName=Cluster0
