# Jobs API Configuration
# Copy this file to .env and fill in your own values

# MongoDB Atlas Connection URI
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>
# Get your URI from MongoDB Atlas:
# 1. Go to Clusters > Connect > Connect your application
# 2. Select "Driver" and copy the connection string
# 3. Replace <username>, <password>, and <cluster> with your actual values
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/jobs_db
